diff --git a/compose.yml b/compose.yml
index 615c300..87880ac 100644
--- a/compose.yml
+++ b/compose.yml
@@ -6,7 +6,7 @@ services:
   mongodb:
     image: mongo:7.0.28-jammy
     restart: unless-stopped
-    command: mongod --bind_ip_all --replSet rs0
+    command: mongod --bind_ip_all --replSet rs0 --wiredTigerCacheSizeGB 2
     healthcheck:
       test: mongosh --quiet /scripts/replica-set.js
       interval: 5s
@@ -22,7 +22,7 @@ services:
   mongodb_secondary:
     image: mongo:7.0.28-jammy
     restart: unless-stopped
-    command: mongod --bind_ip_all --replSet rs0
+    command: mongod --bind_ip_all --replSet rs0 --wiredTigerCacheSizeGB 2
     profiles:
       - base
 
@@ -46,8 +46,9 @@ services:
     environment:
       - LILA_DOMAIN=${LILA_DOMAIN:-localhost:8080}
       - LILA_URL=${LILA_URL:-http://localhost:8080}
-      - MOCK_EMAIL=${MOCK_EMAIL:-true}
-      - ENABLE_MONITORING=${ENABLE_MONITORING:-false}
+      - MOCK_EMAIL=${MOCK_EMAIL:-false}
+      - MAILER_PASSWORD=${MAILER_PASSWORD}
+      - ENABLE_MONITORING=${ENABLE_MONITORING:-true}
       - ENABLE_RATE_LIMITING=${ENABLE_RATE_LIMITING:-true}
     volumes:
       - ./repos/lila:/lila
@@ -65,6 +66,7 @@ services:
     restart: unless-stopped
     ports:
       - 8080:80
+      - 9664:9664
     environment:
       - LILA_DOMAIN=${LILA_DOMAIN:-localhost:8080}
       - LILA_URL=${LILA_URL:-http://localhost:8080}
@@ -80,6 +82,8 @@ services:
     restart: unless-stopped
     ports:
       - 8080:8080
+      - 9664:9664
+      - 9664:9664
     environment:
       - LILA_DOMAIN=${LILA_DOMAIN:-localhost:8080}
       - LILA_URL=${LILA_URL:-http://localhost:8080}
