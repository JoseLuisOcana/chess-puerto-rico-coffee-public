# NOTE: Plesk also maintains an identical copy at
# /var/www/vhosts/system/chesspuertoricocoffee.com/conf/nginx.conf
# Both files are byte-identical as of 2026-04-19.
# If you edit one, update both and run: sudo nginx -t && sudo systemctl reload nginx
